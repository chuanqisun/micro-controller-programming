/**
 * @file env.h.example
 * @brief Example environment configuration template
 * @instructions
 * 1. Copy this file to env.h
 * 2. Fill in your WiFi credentials
 * 3. Do NOT commit env.h to version control
 */

#ifndef ENV_H
#define ENV_H

// WiFi network name (SSID)
const char *WIFI_SSID = "YOUR_WIFI_SSID";

// WiFi password
const char *WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";

#endif
